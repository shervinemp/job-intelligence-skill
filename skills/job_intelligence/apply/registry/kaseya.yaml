name: kaseya
version: 1
detect:
  domains:
    - kaseya.com
handler_class: apply.handlers.standard_form.StandardFormHandler
probe:
  best_strategy: standard
patterns:
  already_applied:
    - "already applied"
properties:
  multi_page: false
  has_eeo: false

name: lever
version: 2
detect:
  domains:
    - lever.co
probe:
  best_strategy: standard
patterns:
  already_applied:
    - "already applied"
  guest_apply:
    - "apply as guest"
    - "continue without signing in"
properties:
  multi_page: false
  has_eeo: false

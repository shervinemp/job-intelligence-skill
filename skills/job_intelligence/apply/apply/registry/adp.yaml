name: adp
version: 1

detect:
  domains:
    - adp.com

probe:
  best_strategy: standard

properties:
  multi_page: true
  has_eeo: true

notes: |
  ADP WorkForce Now — standard HTML forms for job applications. Multi-page
  flow with typical fields (name, email, phone, resume, EEOC, etc.).
  Login/account creation wall common. Pipeline handles via standard probe.
